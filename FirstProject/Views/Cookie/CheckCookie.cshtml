@{
    ViewData["Title"] = "Check Cookie";
    var hasCookie = ViewData["HasCookie"] as bool?;
    var cookieValue = ViewData["CookieValue"] as string;
}

<h2>Check Cookie</h2>

@if (hasCookie == true)
{
    <p>Cookie Value: @cookieValue</p>
}
else
{
    <p>No cookie found or it has expired.</p>
}
